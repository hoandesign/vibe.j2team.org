import type { PageMeta } from '@/types/page'

const meta: PageMeta = {
  name: 'So sánh JSON',
  description: 'So sánh hai JSON dạng side-by-side, hỗ trợ format và copy diff.',
  author: 'area-dev',
  category: 'tool',
}

export default meta
