import type { PageMeta } from '@/types/page'

const meta: PageMeta = {
  name: 'Check IP Location',
  description: 'Check the location of an IP address',
  author: 'Cavaldos',
  category: 'tool',
}

export default meta
